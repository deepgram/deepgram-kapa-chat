export interface ChatConfig {
  /** Kapa React SDK integration ID */
  integrationId: string;
  /** DOM element ID for the "Ask AI" button */
  buttonId: string;
  /** DOM element ID for the inline chat container */
  containerId: string;
}
